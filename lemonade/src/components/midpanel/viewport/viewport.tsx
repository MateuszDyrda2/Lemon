import { ViewportCanvas, ViewportContainer } from './viewport.styles';

const Viewport = () => {
    return (
        <ViewportContainer>
            <ViewportCanvas />
        </ViewportContainer>
    );
};

export default Viewport;
