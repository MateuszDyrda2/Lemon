#pragma once

#include <river/core/basic_types.h>

namespace river {
class batch_renderer
{
  public:
    batch_renderer(size_type maxBatches, size_type maxVertices);

  private:
};
} // namespace river